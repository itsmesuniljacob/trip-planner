# Environment variables for Docker development containers
# This file is used when running the application in Docker development mode

# Database configuration for Docker Compose (development)
DATABASE_URL="postgresql://postgres:password@postgres:5432/trip_planner_dev?schema=public"

# Application configuration
NODE_ENV=development
PORT=3001

# Development specific settings
PRISMA_CLI_QUERY_ENGINE_TYPE=binary